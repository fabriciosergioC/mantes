# Mantes - Variáveis de Ambiente (Netlify + Neon)

# PostgreSQL (Neon)
# Obtenha em: https://neon.tech → Connect → URI
DATABASE_URL=postgresql://user:senha@ep-xxx.us-east-2.aws.neon.tech/mantes?sslmode=require

# JWT Secret (gerar uma chave segura)
JWT_SECRET=sua_chave_secreta_aqui

# Node
NODE_ENV=production
